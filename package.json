{
  "name": "media-blueprint",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Media channel — a company blueprint on the Vetta platform, in three templates: Naive Short Form v1 (`faceless`), Naive Long Form v1 (`longform`) and Naive Clipping v1 (`clipping`). Data only: the crew runs on the platform's board, media gallery and approval card.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@usenaive-sdk/blueprints": "^0.8.0",
    "@usenaive-sdk/vetta-cli": "^0.4.0",
    "typescript": "5.9.3",
    "vitest": "^3.2.7"
  }
}
